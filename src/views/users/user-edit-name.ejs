<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins&display=swap"
      rel="stylesheet"
    />
    <title>Mellowdyne</title>
    <link rel="stylesheet" href="/css/normalize.css" />
    <link rel="stylesheet" href="/css/styles.css" />
    <link rel="stylesheet" href="/css/register.css" />
    <link rel="stylesheet" href="/css/maring-update.css" />
    <link rel="stylesheet" href="/css/footer.css" />
    <script
      src="https://kit.fontawesome.com/525d227bb1.js"
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
    <%- include('../partials/navbar')%>

    <section class="margin-update">
      <!-- Formulario -->
      <h2 class="register-title">Actualizando tus datos</h2>
      <h4 class="ms-error"></h4>
      <form
        method="POST"
        action="/users/profile/editName/<%= userToEdit.id_users %>?_method=PATCH"
        class="create-form form-holder"
        id="name-form"
      >
        <div class="control-form">
          <label><b>Editar nombre</b></label>
          <input
            id="name"
            type="text"
            name="full_name"
            class="control-form-input icon-placeholder"
            value="<%= locals.oldData ? oldData.full_name : userToEdit.full_name %>"
          />
          <div class="is-invalid"></div>
          <% if (locals.errors && errors.full_name) { %>
          <div class="text-danger"><%= errors.full_name.msg %></div>
          <% } %>
        </div>

        <div class="btn-form">
          <button type="submit" class="form-btn">Actualizar datos</button>
        </div>
      </form>
    </section>

    <%- include('../partials/footer')%>
    <script src="/js/index.js"></script>
    <script src="/js/validateUpdateName.js"></script>
  </body>
</html>
